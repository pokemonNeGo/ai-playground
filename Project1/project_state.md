📊 Project1 State: RAG End-to-End Pipeline

🎯 Цель проекта
Построить работающий конвейер «документы → ответы с цитатами» для большой базы документов (5000+) и измерить влияние проектных решений на качество поиска и генерации.
Итоговый артефакт: репозиторий с API (FastAPI), схемой архитектуры и таблицами измерений.

📅 Прогресс выполнения
| Лабораторная | Название | Статус | Дата | Ключевые решения |
| --- | --- | --- | --- | --- |
| 1.1 | Выбор и загрузка корпуса | ✅ Выполнена | 2026-09-17 | Русская Википедия (wikimedia/wikipedia, 20231101.ru), 5000 статей, JSONL |
| 1.2 | Парсер документов | ✅ Выполнена | 2026-09-17 | `parse()` / `safe_parse()`; regex-очистка wiki-разметки, пустых скобок и пробелов; логирование ошибок в `lab1_2_parse_errors.jsonl`; распарсено 5000/5000, все маркеры качества = 0 |
| 1.3 | Нормализация и дедупликация | ⏳ Не начата | - | - |
| 2.1 | Chunking: фиксированный размер | ⏳ Не начата | - | - |
| 2.2 | Chunking: структурное | ⏳ Не начата | - | - |
| 2.3 | Эмбеддинги и Qdrant | ⏳ Не начата | - | - |
| 2.4 | BM25-индекс | ⏳ Не начата | - | - |
| 3.1 | Плотный поиск | ⏳ Не начата | - | - |
| 3.2 | Гибридный поиск (RRF) | ⏳ Не начата | - | - |
| 3.3 | Реранкинг | ⏳ Не начата | - | - |
| 3.4 | Генерация с цитатами | ⏳ Не начата | - | - |
| 4.1 | Инкрементальное обновление | ⏳ Не начата | - | - |
| 4.2 | Parent-child чанки | ⏳ Не начата | - | - |
| 4.3 | Сравнительный эксперимент | ⏳ Не начата | - | - |
| 4.4 | Итоговая документация | ⏳ Не начата | - | - |

1. 🏗 Архитектурные решения

1.1  Источник данных
Корпус: Russian Wikipedia subset, Hugging Face `wikimedia/wikipedia`
Конфигурация: `20231101.ru`
Количество документов: 5000
Формат документов: JSON Lines; поля `doc_id`, `title`, `text`, `source`, `date`, `metadata`

1.2 Хранилище документов
Формат сырого среза: JSONL
Путь к сырому срезу: `data/raw/ru_wikipedia_5000.jsonl`
Статистика Лаб 1.1: `data/processed/lab1_1_corpus_stats.json`
Распарсенный корпус (Лаб 1.2): `data/processed/ru_wikipedia_5000_parsed.jsonl`, 5000 документов, failed = 0
Ошибки парсинга (Лаб 1.2): `data/processed/lab1_2_parse_errors.jsonl` (пустой: 0 ошибок)
Статистика Лаб 1.2: `data/processed/lab1_2_parse_stats.json`
Лог Лаб 1.2: `data/processed/lab1_2_parse.log`
Итоговый формат хранения (SQLite/Parquet) выберем в Лаб 1.3
Хранение в Git: правило `data/` в корневом `.gitignore` исключает всю папку `data/`, поэтому в Git НЕ хранится ни один файл из перечисленных выше, включая статистики `.json` и логи
Также НЕ хранятся в Git: `.env`, `.venv/`, `venv/`, `__pycache__/`, `*.py[cod]`, `*.sqlite3`, `*.parquet`, `qdrant_storage/`, `.idea/`, `.vscode/`, `.orca-preparing/`
Хранятся в Git: `src/`, `scripts/`, `tests/`, `notebooks/`, `project_state.md`, `.gitignore`, `README.md`
Примечание: так как исключена вся родительская папка `data/`, точечные `!`-исключения для файлов внутри неё в Git не работают. Если захотим держать статистики в Git, нужно менять правило `data/` на точечные паттерны (решение примем в Лаб 1.3)
Воспроизводимость: все артефакты `data/processed/` пересоздаются повторным запуском скриптов Лаб 1.1/1.2, поэтому их отсутствие в Git не мешает работе

1.3 Парсер (Лаб 1.2)
Точка входа: `parse(doc) -> {"text": ..., "metadata": ...}`, файл `src/ingestion/parser.py`
Безопасная обёртка: `safe_parse(doc) -> (result | None, error | None)` — один повреждённый документ не прерывает обработку корпуса
Очистка: wiki-ссылки, шаблоны, `<ref>`, HTML-теги, URL, пустые скобки вида `( , , , )`, нормализация пробелов и переносов
Ошибки: `ParserError` + логирование в консоль и файл; построчные ошибки пишутся в `lab1_2_parse_errors.jsonl`
Контроль качества: `scripts/lab1_2_check_quality.py`; финальный отчёт по 5000 документов: все маркеры разметки = 0

1.4 Chunking стратегии
Стратегия A (фиксированный размер):
Размер чанка: [определится в Лаб 2.1] токенов
Перекрытие (overlap): [определится в Лаб 2.1] токенов
Стратегия B (структурное):
Метод разбиения: [по заголовкам/абзацам - определится в Лаб 2.2]
Parent-child (Лаб 4.2):
Parent chunk size: [определится]
Child chunk size: [определится]

1.5 Эмбеддинги
Модель: [bge-m3/e5-multilingual - определится в Лаб 2.3]
Размерность вектора: [определится автоматически]
Batch size для обработки: [определится]

1.6 Векторная база данных
Тип: Qdrant
Способ запуска: [Docker/In-memory - определится в Лаб 2.3]
Название коллекции: rag_documents
Метрика сходства: [Cosine/Dot product]

1.7 Текстовый поиск (BM25)
Реализация: [rank-bm25/SQLite FTS5 - определится в Лаб 2.4]

1.8 Retrieval параметры
Top-k для первичного поиска: [определится в Лаб 3.1]
Top-k после реранкинга: [определится в Лаб 3.3]
Метод гибридного поиска: RRF (Reciprocal Rank Fusion)
RRF k параметр: [определится, обычно 60]

1.9 Реранкинг
Модель: [кросс-энкодер - определится в Лаб 3.3]

1.10 LLM для генерации
Провайдер: [Groq/Ollama/OpenAI - определится в Лаб 3.4]
Модель: [определится]
Temperature: [определится, обычно 0.1-0.3 для RAG]
Max tokens: [определится]

2. 📁 Структура проекта
Весь курс живёт в одном репозитории. В нём есть тренировочные лабораторные
(lab01–lab03) и основные проекты (на данный момент Project1).
Общие для всего репозитория: .venv, .gitignore, README.md.
Все пути в этом файле дальше считаются относительно папки Project1/.

ai-playground/
├── .venv/                      # общее виртуальное окружение для всех лаб (в Git не хранится)
├── .gitignore                  # общие правила игнорирования
├── README.md                   # общий README
│
├── lab01/                      # тренировочная лаба 1
├── lab02/                      # тренировочная лаба 2
├── lab03/                      # тренировочная лаба 3
│
└── Project1/                   # Основной проект1
    ├── project_state.md        # этот файл, состояние проекта (хранится в Git)
    ├── .env                    # API-ключи (появится в Лаб 2.3/3.4; в Git не хранится)
    │
    ├── data/                   # ВСЯ папка не хранится в Git (правило `data/` в .gitignore)
    │   ├── raw/                # сырые документы
    │   │   └── ru_wikipedia_5000.jsonl        # Лаб 1.1 ✅
    │   ├── processed/          # обработанные данные и статистика
    │   │   ├── lab1_1_corpus_stats.json       # Лаб 1.1 ✅
    │   │   ├── ru_wikipedia_5000_parsed.jsonl # Лаб 1.2 ✅
    │   │   ├── lab1_2_parse_errors.jsonl      # Лаб 1.2 ✅ (пустой)
    │   │   ├── lab1_2_parse_stats.json        # Лаб 1.2 ✅
    │   │   └── lab1_2_parse.log               # Лаб 1.2
    │   └── indexes/            # BM25-индексы (появится в Лаб 2.4)
    │
    ├── notebooks/              # ноутбуки лабораторных
    │   ├── lab1_1_corpus_exploration.ipynb    # Лаб 1.1 ✅
    │   ├── lab3_1_search_analysis.ipynb       # Лаб 3.1 (позже)
    │   └── lab4_3_comparison.ipynb            # Лаб 4.3 (позже)
    │
    ├── src/                    # исходный код (хранится в Git)
    │   ├── __init__.py                          # Лаб 1.2 ✅
    │   ├── ingestion/
    │   │   ├── __init__.py                      # Лаб 1.2 ✅
    │   │   └── parser.py                        # Лаб 1.2 ✅
    │   ├── chunking/           # fixed_size.py, structural.py, parent_child.py (Лаб 2.1-2.2)
    │   ├── indexing/           # embeddings.py, qdrant_index.py, bm25_index.py (Лаб 2.3-2.4)
    │   ├── retrieval/          # dense_search.py, hybrid_search.py, reranker.py (Лаб 3.1-3.3)
    │   ├── generation/         # llm_generator.py (Лаб 3.4)
    │   └── api/                # main.py (FastAPI, Лаб 4.4)
    │
    ├── scripts/                # хранится в Git
    │   ├── lab1_2_parse_corpus.py               # Лаб 1.2 ✅
    │   └── lab1_2_check_quality.py              # Лаб 1.2 ✅
    ├── tests/                  # хранится в Git
    │   └── test_parser.py                       # Лаб 1.2 ✅ (8 тестов)
    └── docs/                   # документация и схема архитектуры (Лаб 4.4)

3. 📦 Установленные библиотеки

Core
Python: [версия, например 3.11]
FastAPI: [версия] - для API
Uvicorn: [версия] - ASGI сервер

ML & NLP
sentence-transformers: [версия] - для эмбеддингов
rank-bm25: [версия] - для текстового поиска
transformers: [версия] - для реранкера
torch: [версия] - PyTorch backend

Vector DB
qdrant-client: [версия] - клиент Qdrant

Data Processing
datasets: [версия] - загрузка HuggingFace datasets (использовалась в Лаб 1.1)
pandas: [версия] - обработка данных (использовалась в Лаб 1.1)
numpy: [версия] - численные операции
pyarrow: [версия] - бэкенд для pandas/datasets

Utilities
python-dotenv: [версия] - переменные окружения
tqdm: [версия] - прогресс-бары (использовалась в Лаб 1.1)
matplotlib/seaborn: [версия] - визуализация
pydantic: [версия] - валидация данных
jupyter: [версия] - для работы с notebooks

Testing
pytest: [версия] - юнит-тесты (добавлена в Лаб 1.2)

4. 🔐 Переменные окружения (.env)

Qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333

LLM Provider (Groq/Ollama/OpenAI)
LLM_PROVIDER=groq
LLM_API_KEY=your_api_key_here
LLM_MODEL=llama-3.1-70b-versatile

Embedding Model (если используется API)
EMBEDDING_MODEL=BAAI/bge-m3

Paths
DATA_DIR=./data
MODELS_DIR=./models

5. 📊 Схемы данных

Document Schema (Лаб 1.2-1.3)
Реализована в Лаб 1.2: документы в `data/processed/ru_wikipedia_5000_parsed.jsonl`
соответствуют этой схеме. Особенности: `date` часто null (особенность среза);
в `metadata` добавлен блок `parser`: {"lab", "version", "text_chars"}.
{
"doc_id": str,           # Уникальный ID документа
"title": str,            # Заголовок
"text": str,             # Полный очищенный текст
"source": str,           # Источник (URL)
"date": str,             # Дата публикации (в текущем срезе часто null)
"metadata": dict         # Дополнительные метаданные, включая блок parser
}

Chunk Schema (Лаб 2.1-2.2)
{
"chunk_id": str,         # Уникальный ID чанка
"doc_id": str,           # ID родительского документа
"text": str,             # Текст чанка
"chunk_index": int,      # Порядковый номер в документе
"start_char": int,       # Начальная позиция в документе
"end_char": int,         # Конечная позиция в документе
"metadata": dict         # Наследует метаданные документа
}

Parent-Child Schema (Лаб 4.2)
{
"parent_chunk_id": str,  # ID большого чанка
"child_chunk_ids": list, # Список ID мелких чанков
"parent_text": str,      # Текст для контекста LLM
"child_texts": list      # Тексты для поиска
}

6. 📈 Метрики и измерения

Retrieval Quality (Лаб 3.1-3.3)
Precision@k: [измерится]
Recall@k: [измерится]
MRR (Mean Reciprocal Rank): [измерится]

Generation Quality (Лаб 3.4)
Faithfulness: [оценка - ответы только по контексту]
Answer Relevance: [оценка - релевантность ответа вопросу]
Context Relevance: [оценка - релевантность контекста]

Performance
Latency (search): [мс]
Latency (generation): [мс]
Cost per query: [$]

7. ⚠️ Известные проблемы и ограничения
Поле `date` в текущем срезе Википедии пустое, так как в снапшоте 20231101.ru не хранятся даты публикации/редактирования конкретных статей.
Подсчёт слов в статистике выполнен простым методом `.split()`, что даёт приблизительную оценку. Для точной токенизации потребуется отдельная библиотека (tiktoken/huggingface tokenizers) в следующих лабораторных.
В корпусе присутствуют страницы-неоднозначности («Волга может означать: …»); их фильтрацию откладываем до Лаб 1.3. Качество очистки текста контролируется скриптом `scripts/lab1_2_check_quality.py`.
Очистка wiki-разметки выполнена регулярными выражениями с fallback-проходами для вложенных конструкций; остаточные маркеры по отчёту равны 0, но теоретически особо сложная вложенность может вычищаться с потерей окружающего текста — контролируем тестами и check_quality.py.
Статистики лабораторных (`lab1_1_corpus_stats.json`, `lab1_2_parse_stats.json`) не хранятся в Git из-за правила `data/` в `.gitignore`; при потере локальных файлов они пересоздаются повторным запуском соответствующих скриптов.

8. 🎓 Ключевые инсайты
Загрузка больших датасетов через `streaming=True` позволяет работать с корпусами без полного скачивания их на диск.
Дедупликация по `doc_id` и `title` на этапе загрузки критична: в дампах Википедии часто встречаются страницы-редиректы и дубликаты.
Фильтрация по минимальной длине (`MIN_CHARS = 300`) отсекает служебные страницы и слишком короткие заготовки.
После удаления wiki-ссылок и шаблонов в тексте остаются «пустые» скобки вида `( , , , )` (6204 шт. до правки); добавлена пост-очистка пустых скобок и fallback для вложенных конструкций, после чего все маркеры разметки в отчёте качества равны 0.
Паттерн `safe_parse()` с возвратом `(result, error)` позволяет не ронять весь прогон корпуса из-за одного битого документа и копить все ошибки в отдельном JSONL для разбора.
Автоматический отчёт маркеров разметки (check_quality.py) находит артефакты парсера, которые легко пропустить при ручном просмотре файла.

7. 📝 Заметки для следующих сессий
Лаб 1.2 выполнена: парсер `src/ingestion/parser.py`, тесты `tests/test_parser.py` зелёные (8 тестов), корпус распарсен 5000/5000, failed = 0.
Готово к Лаб 1.3: `data/processed/ru_wikipedia_5000_parsed.jsonl`.
План Лаб 1.3: нормализация, дедупликация, фильтр страниц-неоднозначностей, выбор итогового формата хранения (SQLite/Parquet).
Хранение в Git по фактическому корневому `.gitignore`: игнорируются вся папка `data/` (включая статистики и логи), `.env`, `.venv/`, `venv/`, `__pycache__/`, `*.py[cod]`, `*.sqlite3`, `*.parquet`, `qdrant_storage/`, `.idea/`, `.vscode/`, `.orca-preparing/`.